export default function BookmarkCard({ bookmark, onEdit, onDelete, onFavorite }) {
  return <article className="bookmark-card"><div className="card-top"><span className="category">{bookmark.category}</span><button className={`star ${bookmark.isFavorite ? 'active' : ''}`} onClick={() => onFavorite(bookmark.id)} aria-label="Toggle favorite">★</button></div><h3>{bookmark.title}</h3><a href={bookmark.url} target="_blank" rel="noreferrer" className="bookmark-url">{bookmark.url}</a>{bookmark.description && <p>{bookmark.description}</p>}<div className="card-actions"><a className="button secondary" href={bookmark.url} target="_blank" rel="noreferrer">Open</a><button className="button secondary" onClick={() => onEdit(bookmark)}>Edit</button><button className="button danger" onClick={() => onDelete(bookmark)}>Delete</button></div></article>;
}
